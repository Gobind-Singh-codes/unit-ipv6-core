# RFC 8200 — Status and Gap Analysis

## 0. Latest full run

**Exhaustive run:** `results/2026-09-24T13-21-23` (all ordered permutations, ~1986 cases)
**Scoreboard:** PASS 1643 · FAIL 343 · INCONCLUSIVE 0 · NOT_APPLICABLE 0 · ERROR 0
**Notes:** the initial run was externally killed at 99% (L6-2380; not a code
fault — the tail behaved identically on resume); the final 25 cases ran as a
filtered tail with `IP-01-001` up front so delivery-proof context matched, then
merged (see `merged_from` in `summary.json`). AH/ESP singleton cases live only
in the core/security profiles (NOT_APPLICABLE without IPsec context), not here.
Core-profile run below for the deep-dive; §2.3 exhaustive item is CLOSED.

**Run:** `results/2026-09-23T17-58-10`
**Command:** `uv run RFC-8200-conformance.py --interface host0 --source fd:33:33:33:7446:4ff:fe34:5702 --target fd:33:33:33::1 --verbose`
**Profile:** `core` (default) · **Delivery proof:** `auto` · **Seed:** `0`
**Scoreboard:** PASS 4 · FAIL 44 · NOT_APPLICABLE 2 · INCONCLUSIVE 0 · ERROR 0

Every verdict below is traceable to a per-test JSON + pcap in the run directory
(`IP-<id>.json`, `pcap/IP-<id>.pcap`, `summary.json`, `IP-01-matrix.json` with
46 executed matrix entries).

---

## 1. Current status: fully decided, nothing hedging

The suite reaches a defensible verdict on all 50 cases. Each FAIL carries its
receipt: `tx_seen_on_wire`, `liveness_src`, the DUT's exact ICMPv6 Type/Code/pointer,
and the captured packets.

### 1.1 What passes (4)

| Test | Evidence | Meaning |
|---|---|---|
| IP-01-001 (HBH alone) | `IP-01-001.json`: correlated Echo Reply, `tx_seen=true` | DUT processes Hop-by-Hop + PadN |
| IP-01-004 (Fragment alone) | `IP-01-004.json`: correlated Echo Reply | DUT processes a lone Fragment header |
| IP-01-103 (HBH → Fragment) | `IP-01-103.json`: correlated Echo Reply | DUT processes this two-header order |
| IP-03 (unknown opt, action `01`) | `IP-03.json`: TX on wire + IP-01 control PASS in-run, no error seen | Required silent discard holds |

### 1.2 FAIL cluster A — valid packets answered with ICMPv6 errors (23)

The DUT errors instead of processing. Pointers are quoted from the deviations:

- `T4C2 ptr=50` ×1 — IP-01-002 (DestOpt with PadN **only**; our TX bytes
  `3a 00 01 04 …` are textbook-valid, so the option-pointer is the DUT's own reading).
- `T4C0` (erroneous header) ×17 — ptrs 48/56/66/82/90/98/114, covering the lone
  Routing header (IP-01-003), pairs (102, 111–113, 118), repeats (205–207),
  mixed (305, 306, 308, 309), triples (403, 404, 406), and IP-05 (RH0, ptr 66).
- `T4C1` (unrecognized Next Header) ×5 — ptr 40 (201, 202, 301: repeated-HBH
  chains) and ptr 6 (116, 310: the IPv6 header's own Next Header octet, even
  though the flagged type, Fragment/HBH, is accepted elsewhere — cf. 004 PASS).

Pattern: anything containing Destination Options or Routing headers is rejected;
repetition is rejected; some two-header orders containing Fragment are rejected.

### 1.3 FAIL cluster B — mandatory behavior absent, delivery proven (21)

`tx_seen_on_wire=true` plus in-run liveness (chain starts at `IP-01-001`) per test JSON:

- 19 IP-01 chains (101, 106–108, 117, 203, 204, 208, 302–304, 307, 311, 312,
  400–402, 405, 407): no reply at all.
- IP-02 / IP-04: required Parameter Problem Code 2 never sent
  (`IP-02.json`, `IP-04.json`: `dut_packets: 0`).

### 1.4 NOT_APPLICABLE (2)

IP-01-005 / IP-01-006 (AH / ESP singletons): no IPsec security context exists,
so the condition cannot be constructed. Correctly not fabricated.

### 1.5 DUT reading (observation, not a verdict on intent)

The DUT processes HBH, Fragment, and H→F, and correctly discards action-`01`
options — but rejects all Destination Options (even PadN-only), all Routing
headers (even SegLeft-0-ignore cases), repeated headers, and never emits
Parameter Problem for unknown options. This is the signature of **firewall-style
extension-header hardening**, not a broken stack. Whether that policy is
acceptable for this DUT's role is an operator decision; the FAILs are the
accurate conformance record either way.

---

## 2. Gaps — what still needs to be done

### 2.1 Coverage gap: multicast-destination variants (IP-02, IP-04)

- **IP-02** (action `11`) requires the error *only if the destination was not
  multicast. The suite proves the unicast half (FAIL: no error sent) but never
  sends the multicast half (which must be discarded *without* an error).
- **IP-04** (action `10`) requires the error *regardless* of multicast. Only the
  unicast half is exercised; the multicast-destination case is untested.
- Action: add `--mcast-dst`-driven negative cases (multicast L2 mapping already
  exists in `common/packets.py:eth_dst_for_ipv6`) withTx-on-wire + control
  reasoning; expect IP-02-mcast → PASS-if-silent, IP-04-mcast → FAIL-if-silent.

### 2.2 Coverage gap: IPv6 base-header fields

`PROJECT.md §14` lists base-header fields, payload length, Hop Limit, Traffic
Class, and Flow Label. The current IP-01 matrix covers extension-header
ordering/repetition only; malformed/truncated headers, invalid payload-length,
and header-field edge cases have no tests yet. These are new test IDs (not ER
expansions — label them `RFC-CONTEXT` per `ER-REFERENCE.md` scope rules, since
Table-3 contains only IP-01…IP-05).

### 2.3 Coverage gap: exhaustive and security profiles never run live

The evidence (`IP-01-matrix.json`, 46 entries) is the **core** profile. Not yet executed:

- 120 ordered triples (`IP-01-400…519` generator exists, only 8 sampled),
- 4/5/6-header permutations (360/720/720, `matrix_long`, bounded by
  `IP01_MAX_CHAIN_LENGTH`),
- security profile (AH/ESP chains need a real IPsec context; currently
  NOT_APPLICABLE by design).

The ER phrase "any order and any number" is therefore sampled, not exhausted —
correctly recorded, but a longer maintenance-window run of `--profile exhaustive`
is still outstanding.

### 2.4 Interpretation gap: the DUT's odd pointers

`ptr=50` on a PadN-only option and `ptr=6` flagging a Next Header the DUT
accepts elsewhere suggest the DUT parses (or reports) differently from a
reference stack. Worth a vendor query and, per the annexure rule, Helpdesk
confirmation where implementation context differs from the ER's RFC. The raw
material is preserved (`pcap/IP-01-002.pcap`, `pcap/IP-01-116.pcap`).

### 2.5 Method debt (small, 8200-scoped)

- Silence-FAILs rest on `auto` delivery proof (TX + liveness). A `--delivery-proof
  strict` re-run is the available conservative cross-check; keep both runs if the
  result is disputed.
- `dut_packets` ledger is capped at 10 summaries per test — enough for this run
  (max observed: 1–2 DUT packets), revisit if a noisier network is used.
- BPF is now test-traffic-only; re-verify `tx_seen` stays `true` on a quieter
  lab (it was validated on this noisy one, so risk is low).

---

## 3. Next actions (8200 only)

1. [ ] Add multicast-destination cases for IP-02 / IP-04 (§2.1).
2. [ ] Run `--profile exhaustive` in a maintenance window; archive matrix + pcaps (§2.3).
3. [ ] Draft `RFC-CONTEXT` header-field/malformed tests (§2.2) — new IDs, not ER renames.
4. [x] RH0 interpretation applied (§4). Remaining: vendor/Helpdesk note on pointer
   anomalies, citing pcaps.
5. [ ] Regression re-run after any DUT policy change; attach `summary.json` +
   disputed `<ID>.json` + `pcap/<ID>.pcap` + `prereqs.json` (§8 of README).

---

## 4. Applied interpretation: Routing Type 0 drop/reject is PASS (post-17-58-10)

The ER for IP-01 requires *accepting and attempting to process* extension headers
in any order and number — it does not demand successful processing. A
sub-millisecond Parameter Problem proves the DUT walked the chain and handed a
reason: that is the attempt. Since our `R` builder emits Routing Type 0, which
is deprecated per RFC 8200 §4.4 / RFC 5095, rejection-with-reason (or silent
drop of a must-drop packet) is the required outcome.

Rule (implemented in `classify(..., deprecated=True)`, type-locked to the
builder emitting Type 0 via `common/packets.py:ROUTING_HDR_TYPE`):

| Behavior on RH0 stimulus | Verdict |
|---|---|
| ICMPv6 error (any Type/Code/ptr) | PASS — "accepted and attempted processing; returned …" |
| Silence, delivery proven | PASS — deprecated-header drop |
| Echo Reply | PASS — processed end-to-end, ER literally met; 5095 tension noted |
| Silence, delivery unproven | INCONCLUSIVE (unchanged) |

ER text is untouched in every JSON; the mapping lives in a separate
`interpretation` field per `constraints.md §2`. Replaying run 17-58-10 through
the new rule flips exactly the 21 R-bearing cases (003, 102, 107, 111–113, 118,
205, 206, 305–308, 400, 402–407, IP-05) to PASS — predicted scoreboard on
re-run: **PASS 25 / FAIL 23 / NOT_APPLICABLE 2**.

Honest consequence: the RH0 cases are now **non-discriminating** — every
observable outcome passes. Their value is the Type/Code/ptr fingerprint per
chain (regression signal if firmware changes *which* reason is handed). The
suite's discriminating power rests on the non-RH0 chains: repetition
(201, 202, 207, 208), F↔H orders (116, 309, 310), DestOpt handling, and the
unknown-option errors (IP-02/04).
